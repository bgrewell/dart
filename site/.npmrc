# The lockfile's transitive dependencies set a Node floor (undici, via
# unifont, needs >=22.19). Without this the floor is only a warning, and a
# too-old Node fails later with a confusing runtime error instead.
engine-strict=true
